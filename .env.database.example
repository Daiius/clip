# MySQL の資格情報（db コンテナと server が共有する）。
# 実ファイル `.env.database` は gitignore 対象。値は自分で決めること。
MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_DATABASE=clip
MYSQL_USER=clip
MYSQL_PASSWORD=change-me

# server から見た接続先（compose 網内のサービス名。ホストには公開しない。prd/01 §3）
DB_HOST=db
DB_PORT=3306
