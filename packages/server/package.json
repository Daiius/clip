{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Hono(RPC) API・DB スキーマ・BlobStore・認証。",
  "exports": {
    "./app": "./src/app.ts",
    "./limits": "./src/limits.ts"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx migrate.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsx esbuild.config.ts",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "catalog:",
    "@hono/node-server": "catalog:",
    "@hono/zod-validator": "catalog:",
    "drizzle-orm": "catalog:",
    "hono": "catalog:",
    "mysql2": "catalog:",
    "ulid": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "esbuild": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
