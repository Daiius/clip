# 05. ロードマップ

> 本章の内容は**全て計画**（実装は未着手）。実装が進んだら「実装済み」を書き分ける。

## Phase 1（MVP）

**目標: remote dev 環境で、Windows / MacBook / iPhone から実際に受け渡しができる状態。**

| # | 内容 | 関連 |
|---|---|---|
| 1 | monorepo 雛形・compose（db / seaweedfs / server / web）・`.env.*.example` | [01](./01-architecture.md) |
| 2 | `clips` スキーマと Drizzle 設定 | [02](./02-data-model.md) |
| 3 | cookie セッション認証（`/login` とルートガード）+ **ログインのレート制限** | [04](./04-auth-and-privacy.md) §2 |
| 4 | BlobStore（S3 互換）と画像の保存・配信 + **形式 allowlist とシグネチャ検証** | [02](./02-data-model.md) §3-4 |
| 5 | 投入（ページへのペースト / 貼り付けボタン / D&D / ファイル選択） | [03](./03-ux.md) §1 |
| 6 | 一覧・プレビュー・クリックでコピー・ダウンロード・削除 | [03](./03-ux.md) §2-4 |
| 7 | remote dev 公開（`.env.remote` による差分集約） | [01](./01-architecture.md) §4 |

## 未実装・やらないこと

**いずれも「後から足せる形にしておく」ことだけを設計上の要件とし、MVP では作らない。**

| 項目 | 作らない理由 | 阻害しないための担保 |
|---|---|---|
| 検索 | 受け渡し用途では目的のものは最上部にある。溜まって困ってから足す | 一覧 API がページング前提なので、条件を足せる |
| タグ・タイトル | 入力の手数が増え、即保存（[03](./03-ux.md) §1）と相性が悪い | `clips` に列を足すだけで済む |
| 自動失効 | 「消えて困った」事故を原理的に起こさないため | 期限列を足せば実装できる |
| サムネイル生成 | 画像処理依存と生成失敗時のフォールバック設計を持ち込まないため | 配信が `/api/clips/:id/blob` に一本化されているので、派生を足せる |
| 任意ファイル | 「貼ってプレビューできる」性質から外れる | `kind` が enum なので値を足せる |
| **他者への共有** | MVP では自分しか使わない | clip ID とは**独立した共有トークン**（暗号学的乱数・対象・権限・失効を持つ）を後から足せる。**ULID を共有 URL の防壁にしない**（[02](./02-data-model.md) §2 / [04](./04-auth-and-privacy.md) §3） |

## 確定事項

grill による決定（詳細は [`_grilling/decisions.md`](./_grilling/decisions.md)）。**これらは「まだ決めていないこと」
ではなく、意図して選んだ結論である。**

- **貼ったものは自動で消さない。** 消すのは自分の意思による削除だけ。
- **扱うのはテキストと画像のみ。**
- **1エントリはテキストか画像のどちらか一方。** 同居させない。
- **認証はアプリ側の cookie セッション。** 前段プロキシに依存しない。
- **画像の実体は SeaweedFS、メタデータは MySQL。**
- **一覧は新しい順の時系列のみ。**
- **サムネイルを作らず、原寸を CSS で縮小して表示する。**
- **パッケージは `web` と `server` の2つ。** `shared` も `database` も作らない。
